node_modules/
build/
# dist is intentionally NOT ignored so Render can fallback to committed assets
# if vite build OOMs. Build artifacts are still generated via `npm run build`.
# To ignore dev build noise locally, uncomment next line:
# dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local/private files
local.properties
android/app/google-services.json
build-error.txt
APK/
